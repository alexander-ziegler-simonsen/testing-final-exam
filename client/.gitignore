# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vitest browser-mode report attachments (content-hashed, regenerated per run).
.vitest-attachments/

# Vitest browser-mode screenshot baselines (toMatchScreenshot). Machine/
# browser-specific and can be flaky between runs on the same machine
# (animations, focus rings, etc.), so they're regenerated locally rather
# than committed.
# __screenshots__/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
