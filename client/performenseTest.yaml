
services:
  vitest-browser:
    build:
      context: .
      dockerfile: Dockerfile.performance
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm run test:browser
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G