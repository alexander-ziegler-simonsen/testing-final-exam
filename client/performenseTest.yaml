
services:
  vitest-browser:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm run test:browser
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G