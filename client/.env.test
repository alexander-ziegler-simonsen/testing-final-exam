# loaded on top of .env when running vitest (Vite mode defaults to "test")
# MSW enabled — client/src/main.tsx checks this; the vitest unit/browser
# projects start MSW unconditionally in their own setup files regardless,
# this just keeps the var honest for anything else that reads it.
VITE_API_MOCKING=enabled
