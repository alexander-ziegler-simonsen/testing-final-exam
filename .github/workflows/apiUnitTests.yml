name: API unit tests
on:
    push:
    pull_request:
jobs:
    apiLocalTests:
        environment: monoEnv
        timeout-minutes: 15
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: '10.0.x'

            - name: Restore
              run: dotnet restore apiProjects/apiTesting/hospitalApiTesting.csproj

            - name: Build
              run: dotnet build apiProjects/apiTesting/hospitalApiTesting.csproj --no-restore --configuration Release

            - name: Test
              run: dotnet test apiProjects/apiTesting/hospitalApiTesting.csproj --no-build --configuration Release